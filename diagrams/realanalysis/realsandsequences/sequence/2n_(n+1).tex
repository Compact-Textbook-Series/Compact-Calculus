% !TEX program = xelatex
\documentclass[tikz, crop, border = {2pt 2pt 2pt 2pt}]{standalone}

\usepackage{concmath-otf}
\usetikzlibrary{calc, angles, quotes, patterns}
\usetikzlibrary{decorations.pathreplacing, decorations.pathmorphing, calligraphy}

\begin{document}
\begin{tikzpicture}
	\draw[-stealth, thick] (0, 0) -- (4, 0) node[right]{$n$};
	\draw[-stealth, thick] (0, 0) -- (0, 3) node[above]{$b_n$};

	\foreach \i [count = \xi] in {0, 0.2, 0.4, ..., 4} {
			\filldraw (\i, {2 * (\xi - 1) / (\xi)}) circle (1pt);
		}

	\foreach \i in {0, 0.25, 0.5, ..., 2.5} {
			\draw (1pt, \i) -- ++ (-2pt, 0);
		}
	\foreach \i in {0, 1, 2} {
			\draw (2pt, \i) -- ++ (-4pt, 0) node[left]{\i};
		}
	\draw[dashed] (0, 2) -- ++ (4, 0);
\end{tikzpicture}
\end{document}
